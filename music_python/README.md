## music_python会被其他模块调用，能自己启动，测试的时候可以自己启动

考虑是根据python位置运行，
还是远程调用对应的python程序
### 步骤
1. python程序接到调用程序，获取数据库，
2. 运行python程序，然后将数据中的数据进行一次更新
3. 调用python程序后，java不需要考虑是否结束，但是python会返回一个结束标志，告诉已经结束当前的计算，
4. python 程序输出结果应该在java程序中有出现。

### 