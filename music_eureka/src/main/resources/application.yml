server:
  port: 8761
spring:
  application:
    name: music_eureka
eureka:
  client:
    fetch-registry: false
    register-with-eureka: false
  instance:
    prefer-ip-address: true # 注册服务的时候优先选择IP，不是主机名